{
    "name": "tbs-monorepo",
    "private": true,
    "scripts": {
        "dev": "pnpm -r dev",
        "build": "pnpm -r build",
        "typecheck": "pnpm -r typecheck",
        "lint": "pnpm -r lint",
        "test": "pnpm -r test"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "eslint": "^9.0.0",
        "prettier": "^3.2.0"
    }
}
